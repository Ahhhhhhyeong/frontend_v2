// src/components/CartIcon.jsx (아이콘 컴포넌트를 새로 만듭니다)
// SVG 코드는 Figma에서 'Copy as SVG'로 복사해오세요.

export default function CartIcon() {
    return (
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M...Z" fill="currentColor" />
      </svg>
    );
  }
  
  // 사용할 페이지에서 import 해서 <CartIcon /> 처럼 사용하면 됩니다.